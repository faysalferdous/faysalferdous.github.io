const canvas=document.querySelector('#matrix');const context=canvas.getContext('2d');let drops=[];let fontSize=16;let previous=0;const characters='01アイウエオカキクケコABCDEFGHIJKLMNOPQRSTUVWXYZ';function resizeMatrix(){const ratio=Math.min(window.devicePixelRatio||1,2);canvas.width=window.innerWidth*ratio;canvas.height=window.innerHeight*ratio;canvas.style.width=`${window.innerWidth}px`;canvas.style.height=`${window.innerHeight}px`;context.setTransform(ratio,0,0,ratio,0,0);drops=Array.from({length:Math.ceil(window.innerWidth/fontSize)},()=>Math.random()*-60)}function drawMatrix(timestamp=0){if(timestamp-previous>75){context.fillStyle='rgba(4,7,6,.12)';context.fillRect(0,0,window.innerWidth,window.innerHeight);context.fillStyle='#82f746';context.font=`${fontSize}px ui-monospace,monospace`;drops.forEach((position,index)=>{context.fillText(characters[Math.floor(Math.random()*characters.length)],index*fontSize,position*fontSize);drops[index]=(position*fontSize>window.innerHeight&&Math.random()>.975)?0:position+1});previous=timestamp}requestAnimationFrame(drawMatrix)}resizeMatrix();if(!window.matchMedia('(prefers-reduced-motion: reduce)').matches)drawMatrix();window.addEventListener('resize',resizeMatrix);const toggle=document.querySelector('.menu-toggle');const nav=document.querySelector('nav');toggle.addEventListener('click',()=>{const open=nav.classList.toggle('open');toggle.setAttribute('aria-expanded',String(open))});document.querySelectorAll('nav a').forEach(link=>link.addEventListener('click',()=>nav.classList.remove('open')));document.addEventListener('click',event=>{if(!nav.contains(event.target)&&!toggle.contains(event.target))nav.classList.remove('open')});const items=document.querySelectorAll('.reveal');const revealObserver=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('show');revealObserver.unobserve(entry.target)}}),{threshold:.12});items.forEach(item=>revealObserver.observe(item));const navLinks=[...document.querySelectorAll('nav a')];const sections=[...document.querySelectorAll('main section[id]')];const sectionObserver=new IntersectionObserver(entries=>{const visible=entries.filter(entry=>entry.isIntersecting).sort((a,b)=>b.intersectionRatio-a.intersectionRatio)[0];if(visible)navLinks.forEach(link=>link.classList.toggle('active',link.getAttribute('href')===`#${visible.target.id}`))},{rootMargin:'-30% 0px -60% 0px',threshold:[.01,.2,.5]});sections.forEach(section=>sectionObserver.observe(section));document.querySelector('#year').textContent=new Date().getFullYear();
